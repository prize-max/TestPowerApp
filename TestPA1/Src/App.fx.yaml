App As appinfo:
    BackEnabled: =false
    OnStart: |
        =Set(RefreshFlag,true);
        ClearCollect(TestTablesColleaction, ForAll(TestTables,{SDate:ScheduleDate}));
        //ForAll(TestTablesColleaction,ScheduleDate);
    Theme: =PowerAppsTheme

    Host As hostControl.DefaultHostControlVariant:
        OnCancel: =false
        OnEdit: =false
        OnNew: =false
        OnSave: =false
        OnView: =false

