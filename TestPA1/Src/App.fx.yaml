App As appinfo:
    BackEnabled: =false
    OnStart: =Set(RefreshFlag,true);

    Host As hostControl.DefaultHostControlVariant:
        OnCancel: =false
        OnEdit: =false
        OnNew: =false
        OnSave: =false
        OnView: =false

